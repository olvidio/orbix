openapi: "3.1.0"
info:
  title: "Orbix API - cartaspresentacion"
  version: "0.1.0"
  description: |
    OpenAPI generado desde `docs/catalogo`. Revisar obligatoriedad de campos y ejemplos antes de publicarlo como contrato estable.
  license:
    name: "Proprietary"
    identifier: "LicenseRef-Proprietary"
servers:
  - url: "/"
security:
  - OrbixSession: []
paths:
  "/src/cartaspresentacion/carta_presentacion_eliminar":
    post:
      operationId: "post_cartaspresentacion_carta_presentacion_eliminar"
      summary: "Carta Presentacion Eliminar"
      description: |
        Elimina una `CartaPresentacion`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/carta_presentacion_eliminar.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_ubi"
                - "id_direccion"
              properties:
                id_direccion:
                  type: "integer"
                id_ubi:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/cartaspresentacion/carta_presentacion_form_data":
    post:
      operationId: "post_cartaspresentacion_carta_presentacion_form_data"
      summary: "Carta Presentacion Form Data"
      description: |
        Datos del formulario modal de modificación de una `CartaPresentacion`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/carta_presentacion_form_data.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_ubi"
                - "id_direccion"
              properties:
                id_direccion:
                  type: "integer"
                id_ubi:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartaPresentacionFormDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartaPresentacionFormDataDataResponse"
  "/src/cartaspresentacion/carta_presentacion_update":
    post:
      operationId: "post_cartaspresentacion_carta_presentacion_update"
      summary: "Carta Presentacion Update"
      description: |
        Crea o actualiza una `CartaPresentacion`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/carta_presentacion_update.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_ubi"
                - "id_direccion"
              properties:
                id_direccion:
                  type: "integer"
                id_ubi:
                  type: "integer"
                observ:
                  type: "string"
                pres_mail:
                  type: "string"
                pres_nom:
                  type: "string"
                pres_telf:
                  type: "string"
                zona:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/cartaspresentacion/cartas_presentacion_buscar_data":
    post:
      operationId: "post_cartaspresentacion_cartas_presentacion_buscar_data"
      summary: "Cartas Presentacion Buscar Data"
      description: |
        Opciones de los desplegables del formulario de búsqueda de cartas de presentación.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/cartas_presentacion_buscar_data.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionBuscarOpcionesDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionBuscarOpcionesDataDataResponse"
  "/src/cartaspresentacion/cartas_presentacion_lista_data":
    post:
      operationId: "post_cartaspresentacion_cartas_presentacion_lista_data"
      summary: "Cartas Presentacion Lista Data"
      description: |
        Listado agrupado de cartas de presentación en HTML ya formateado (tipo labor → delegación → población).
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/cartas_presentacion_lista_data.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl:
                  type: "string"
                pais:
                  type: "string"
                poblacion:
                  type: "string"
                que:
                  type: "string"
                region:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionListaDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionListaDataDataResponse"
  "/src/cartaspresentacion/cartas_presentacion_shell_data":
    post:
      operationId: "post_cartaspresentacion_cartas_presentacion_shell_data"
      summary: "Cartas Presentacion Shell Data"
      description: |
        Bootstrap de la pantalla principal `cartas_presentacion`: devuelve la delegación del usuario y las rutas relativas de los demás endpoints del módulo, junto con las especificaciones `hash_*` que el frontend firma con `HashFront`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/cartas_presentacion_shell_data.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionShellDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionShellDataDataResponse"
  "/src/cartaspresentacion/poblaciones_data":
    post:
      operationId: "post_cartaspresentacion_poblaciones_data"
      summary: "Poblaciones Data"
      description: |
        Opciones del desplegable de poblaciones de la pantalla principal según el filtro elegido.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/poblaciones_data.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                filtro:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionPoblacionesDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionPoblacionesDataDataResponse"
  "/src/cartaspresentacion/ubis_lista_data":
    post:
      operationId: "post_cartaspresentacion_ubis_lista_data"
      summary: "Ubis Lista Data"
      description: |
        Listado de centros con el estado (sí/no) de su carta de presentación, para pintar con `Lista`.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/cartaspresentacion/api/ubis_lista_data.md`
      tags:
        - "cartaspresentacion"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                poblacion_sel:
                  type: "string"
                tipo_lista:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionUbisListaDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionUbisListaDataDataResponse"
components:
  securitySchemes:
    OrbixSession:
      type: apiKey
      in: cookie
      name: PHPSESSID
  schemas:
    OrbixStandardResponseEnvelope:
      type: object
      required:
        - success
        - data
      properties:
        success:
          type: boolean
        mensaje:
          type: string
        data:
          description: "Ver schema concreto de cada operacion."
    cartaspresentacion_CartaPresentacionFormDataData:
      type: object
      properties:
        ok:
          type: "boolean"
        mensaje:
          type: "string"
        id_ubi:
          type: "integer"
        id_direccion:
          type: "integer"
        nombre_ubi:
          type: "string"
        pres_nom:
          type: "string"
        pres_telf:
          type: "string"
        pres_mail:
          type: "string"
        zona:
          type: "string"
        observ:
          type: "string"
        paths:
          type: array
          items: {}
        hash_update:
          type: array
          items: {}
    cartaspresentacion_CartaPresentacionFormDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/cartaspresentacion_CartaPresentacionFormDataData"
    cartaspresentacion_CartasPresentacionBuscarOpcionesDataData:
      type: object
      properties:
        opciones_region:
          type: array
          items: {}
        opciones_pais:
          type: array
          items: {}
        opciones_delegacion:
          type: array
          items: {}
        paths:
          type: array
          items: {}
        hash_lista:
          type: array
          items: {}
    cartaspresentacion_CartasPresentacionBuscarOpcionesDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionBuscarOpcionesDataData"
    cartaspresentacion_CartasPresentacionListaDataData:
      type: object
      properties:
        html_lista:
          type: "string"
        html_errores:
          type: "string"
    cartaspresentacion_CartasPresentacionListaDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionListaDataData"
    cartaspresentacion_CartasPresentacionShellDataData:
      type: object
      properties:
        mi_dele:
          type: "string"
        paths:
          type: array
          items: {}
        hash_ctr:
          type: array
          items: {}
        hash_lista:
          type: array
          items: {}
        hash_form:
          type: array
          items: {}
        hash_poblaciones:
          type: array
          items: {}
        hash_eliminar:
          type: array
          items: {}
    cartaspresentacion_CartasPresentacionShellDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionShellDataData"
    cartaspresentacion_CartasPresentacionPoblacionesDataData:
      type: object
      properties:
        id:
          type: "string"
        opciones:
          type: array
          items: {}
        selected:
          type: "string"
        blanco:
          type: "boolean"
        val_blanco:
          type: "string"
        action:
          type: "string"
        clase:
          type: "string"
    cartaspresentacion_CartasPresentacionPoblacionesDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionPoblacionesDataData"
    cartaspresentacion_CartasPresentacionUbisListaDataData:
      type: object
      properties:
        tipo_lista:
          type: "string"
        explicacion:
          type: "string"
        a_cabeceras:
          type: array
          items: {}
        a_valores:
          type: array
          items: {}
    cartaspresentacion_CartasPresentacionUbisListaDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/cartaspresentacion_CartasPresentacionUbisListaDataData"
    OrbixStandardResponseStringData:
      type: object
      required:
        - success
        - data
      properties:
        success:
          type: boolean
        mensaje:
          type: string
        data:
          oneOf:
            - type: string
            - type: object
            - type: array
              items: {}
          description: "En ContestarJson::enviar, los arrays/objetos suelen llegar codificados como string JSON por compatibilidad."
