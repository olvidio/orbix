---
tipo: "progreso_revision"
alcance: "catalogo"
ultima_actualizacion: 2026-07-13
---

# Progreso de revisión del catálogo

Metodología: [`_GUIA_REVISION.md`](_GUIA_REVISION.md). Orden: alfabético. Estado por **capa API**
(la prioritaria); pantallas/flujos es segunda pasada.

Leyenda estado: ⬜ pendiente · 🟡 en curso · ✅ API revisada · ⭐ API+pantallas+flujos revisados.

| # | Módulo | API | Pant | Fluj | Estado | Notas |
|---|--------|-----|------|------|--------|-------|
| 1 | actividadcargos | 5 | 4 | 4 | ⭐ | Piloto de referencia. API revisada (`*_data`→form_data, Salida real, permisos). Pant+flujos revisados jul 2026: widgets dossier 3102/1302, forms AJAX, validación cliente `fnjs_*_datos_ok`, permisos `rstgr`/`des`/`vcsd`; Ruta de menú «sin entrada» (acceso vía dossiers). Regenerados `docs/ai/actividadcargos` y `docs/manual/actividadcargos.md`. |
| 2 | actividades | 32 | 16 | 28 | ⭐ | API revisada (ola 1). Pant+flujos revisados jul 2026: subtipos corregidos (`lista_activ_que`, `tipo_activ`, `actividades_centro_que` → pantalla_principal; `lista_sr_csv_que`/`actividad_nuevo_curso` idem); resúmenes y manual vs `frontend/actividades/`; **Ruta de menú** en 16 pant + 28 flujos (`_referencia_menus.md`); popups/planning sin menú explícito documentados. Regenerados `docs/ai/actividades` y `docs/manual/actividades.md`. |
| 3 | actividadescentro | 7 | 1 | 7 | ⭐ | API+pant+flujos revisados. `operacion` corregida (`lista_actividades_ctr`/`centros_encargados`/`centros_disponibles` → lista_data, `activ_ctr_shell` → form_data; mutaciones ok); Salida real de builders con doble `JSON.parse` (arreglado `centros_disponibles` payload `0`→`tipo/id_activ/centros`); errores `_()` reales (`reordenar`: +`error al ordenar (1..4)`; `num_orden`=dirección `mas/menos`, no número); permisos `ctr` vía `PermisosActividades`/`True`; Ruta de menú (dre/Calendario > actividades > asignar centros + variantes por `tipo`). Frontend `activ_ctr.php`/`.phtml`. |
| 4 | actividadessacd | 14 | 4 | 13 | ⭐ | API+pant+flujos revisados. Asignar sacd (`activ_sacd`): lista/solapes, asignar/reordenar/borrar sacd, candidatos/encargados, auto-asignación; comunicación (`com_sacd_activ_periodo`): listado/envío mails, perm `perm_mod_txt`, textos (`com_sacd_txt`); Ruta de menú (dre/propuestas > asignar sacd + variantes `tipo`; ATENCIÓN SACD > Actividades; comunic. sacd / lista activ. sacd con `propuesta`). |
| 5 | actividadestudios | 27 | 16 | 25 | ⭐ | API+pant+flujos revisados. Linaje `apps/` conservado; actas/matrículas/planes con rutas menú `vest`/ESTUDIOS. |
| 6 | actividadplazas | 11 | 6 | 9 | ⭐ | API+pant+flujos revisados. Flujos: balance (que/dl), resumen/ceder, peticiones (activ/guardar/borrar), incorporar 1ª petición, posibles_propietarios (vía asistentes); rutas menú desde `_referencia_menus.md` o «sin entrada» para fragmentos/actividad. |
| 7 | actividadtarifas | 14 | 9 | 3 | ⭐ | API+pant+flujos revisados. HashB en `tarifa_ubi_*` (`ctx_update`/`ctx_eliminar`/`ctx_copiar`; `requiere_hashb:true`); copiar pendiente reimplementar; `tarifa_ubi_update_inc` claves `{letra}#{id_item}`; permisos `have_perm_oficina('adl'|'pr'|'calendario')`; Ruta menú Tarifas (definir / tarifa-tipo / por casa y año). |
| 8 | asignaturas | 2 | 1 | 1 | ✅ | API revisada: ambos `*_data` reclasificados `mutacion`→`lista_data`; Salida real (`a_opciones` con separador `3000`, `a_asignaturas` id→nombre_corto) y doble `JSON.parse`; sin errores `_()` ni permisos propios; frontend `asig_faltan_que.php`. |
| 9 | asistentes | 15 | 12 | 14 | ⭐ | API+pant+flujos revisados. `operacion` corregida (`*_data` builders → form_data/lista_data; mutaciones ok); Salida real payloads + doble `JSON.parse`; errores `_()` y alias `sel`/`id_pau`/`pau`; permisos `perm_modificar()` en guardar/eliminar/mover/plaza; Ruta de menú en listados (`que_ctr`, `activ_pendientes`, `lista_ultim_*`) y «sin entrada» en dossiers/modales. Regenerados openapi+ai+manual. |
| 10 | cambios | 12 | 6 | 11 | ⭐ | API+pant+flujos revisados. `operacion`: 4 builders `*_data`→form_data/lista_data (`usuario_form_avisos`→lista_data; fases/propiedades/item/preview→form_data); mutaciones ok. Salida real (builders doble `JSON.parse`; mutaciones `data:"ok"` o payload `id_item_usuario_objeto`/`condicion`); errores `_()` reales; permisos frontend+`$_SESSION['oPerm']`/`perm_jefe`. Ruta menú `avisos_generar` (Calendario/sistema/Utilidades); resto embebido sin menú. Frontend `avisos_generar`, `usuario_form_avisos`, `usuario_avisos_pref`+fragmentos.
| 11 | cartaspresentacion | 8 | 5 | 6 | ⭐ | API+pant+flujos revisados. `operacion` corregida (`shell`/`buscar`/`poblaciones`→form_data; `lista`/`ubis`→lista_data); Salida real de builders (shell: `mi_dele`+`paths`+`hash_*`; lista: `html_lista`/`html_errores`; update/eliminar: `data:""` éxito, errores en `mensaje`); errores `_()` reales (form/update/eliminar); permisos dl propia o `cr` en form/update; Ruta menú scdl>direcciones>cartas presentacion (modificar/buscar/lista dl/lista todo). Frontend `cartas_presentacion.php`/`.phtml`. |
| 12 | casas | 15 | 14 | 9 | ⭐ | API+pant+flujos revisados. `operacion` corregida (`casas_resumen`/`calendario_ubi_resumen`/`prevision_asistentes` → lista_data; mutaciones ok); Salida real builders + doble `JSON.parse`; mutaciones ingreso/gastos éxito `data:""`; errores `_()` reales; permisos `adl`/`economic`/`PermisosActividades`; linaje `apps/casas/*_ajax.php`. Ruta de menú en pant/flujos (`_referencia_menus.md`).
| 13 | certificados | 20 | 14 | 18 | ⭐ | API+pant+flujos revisados. `operacion` corregida (`*_data` builders → lista_data/form_data; mutaciones ok; descargas → descarga; `textos_certificados` → include); Salida real payloads + doble `JSON.parse` (uploads JSON directo); errores `_()` reales; permisos `soy_region_stgr()`/ámbito rstgr/frontend+`$_SESSION['oPerm']`/token firmado; Ruta menú lista (`ESTUDIOS > Actas y certificados > Certificados`); resto dossier/modales sin menú. Regenerados openapi+ai+manual.
| 14 | configuracion | 6 | 4 | 4 | ⭐ | API+pant+flujos revisados. Pant: `modulos_select`/`parametros`→pantalla_principal; `modulos_form`/`modulos_update`→fragmento AJAX; vista Twig `parametros.html.twig`; 13 parámetros esquema documentados. Flujos: listado/ficha módulos, parámetros esquema, periodo calendario interno vía `Periodo.php`. Ruta menú `_referencia_menus.md` (definir módulos; config esquema/Esquema). Regenerados docs/ai+manual. |
| 15 | dbextern | 16 | 7 | 16 | ⭐ | API+pant+flujos revisados. Sincronización BDU↔Aquinate: `operacion` corregida (7 builders `*_datos`→form_data/lista_data; 9 mutaciones ok); Salida real (dashboard `sincro_index_datos` contadores+`link_spec_*`; listas JSON urlencoded; mutaciones `data:"ok"` o `{mensaje,count}`); errores `_()` reales; permisos colectivo vía `have_perm_oficina(sm|agd|sg|des)` en bootstrap. Ruta menú `sincro_index` (Actualizar datos desde BDU, variantes `tipo`); subpantallas sin menú directo. Regenerados openapi+ai+manual.
| 16 | devel_db_admin | 16 | 19 | 15 | ⭐ | API+pant+flujos revisados. `operacion`: builders `*_data`→form_data/lista_data (`apptables_apps`, `db_lugar`, `db_propiedades`, `verificar_renombrar`→form_data; `migraciones_lista`→lista_data); mutaciones ok. Salida real payloads + doble `JSON.parse`; mutaciones devuelven `ok`/`avisos`/`lines`/`errores` según caso; `crear_usuarios` mapa passwords; `apptables_update` con hash. Errores `_()`/`RuntimeException` reales; permisos frontend menú devel (sin `perm_*` en casos de uso). Ruta menú 7 entradas `_referencia_menus.md` (DB + Tablas apps); fragmentos/proxies «sin entrada». Regenerados openapi+ai+manual. |
| 17 | dossiers | 6 | 4 | 5 | ⭐ | API+pant+flujos revisados. `operacion` corregida (`*_lista_data`→lista_data, `perm_dossier_ver`/`ver_pantalla`→form_data); Salida real payloads + doble `JSON.parse`; errores `_()` reales; permisos `admin_sv`/`admin_sf` en `PermDossierVerFormData`, resto frontend+`$_SESSION['oPerm']`. Dominios: visor dossiers (`dossiers_ver` lista/ficha, segmentos `select_*`/`datos_tabla`), admin permisos (`perm_dossiers`→`perm_dossier_ver`→`tipo_dossier_*`). Ruta menú `perm_dossiers` (sistema/ADMIN LOCAL > perm_dossiers > p|u|a); dossiers_ver/lista «sin entrada». `lista_dossiers` sin controller propio (parcial de `dossiers_ver`). Script: `aplicar_revision_dossiers.php`. Regenerados `docs/ai/dossiers` y `docs/manual/dossiers.md`. |
| 18 | encargossacd | 34 | 29 | 28 | ⭐ | API+pant+flujos revisados. `operacion`: builders `*_data`→form_data/lista_data (`encargo_select_data`, listas a–d/cl/com_*→lista_data; horario_*_update_data→mutacion pese al sufijo); mutaciones encargo/ficha/ausencias ok. Salida real payloads + doble `JSON.parse`; errores `_()` reales; permisos `des`/`vcsd` en listas c/d y ficha sacd. Linaje `apps/encargossacd/*` (encargo_ajax, ctr_ficha_update, sacd_ficha_ajax, horario_update, sacd_ausencias_update). Ruta menú dre/Encargos + ATENCIÓN SACD (ficha ctr/sacd, ver encargos, listados, propuestas); ausencias sacd; jefe zona exterior. Regenerados openapi+ai+manual. |
| 19 | inventario | 43 | 34 | 42 | ⭐ | API+pant+flujos revisados (ola 4). `operacion` corregida (~30 `lista_data`, builders `form_data`, mutaciones `*_guardar`/equipajes); Salida real payloads + doble `JSON.parse` (`lista_lugares_de_ubi` array directo); errores `_()` en mutaciones; 4 casos `application/` (`EquipajeEliminar`, `InventarioCssInlineData`, `ColeccionesOpcionesData`, `TipoDocOpcionesData`); resto lógica inline en controllers. Dominios: documentos (asignar/trasladar/listados), inventario CTR/DLB impresión, equipajes/EGM/maletas. Ruta menú `_referencia_menus.md` en 10 pant + 8 flujos principales; fragmentos AJAX «sin entrada». Regenerados openapi+ai+manual. |
| 20 | menus | 19 | 7 | 15 | ⬜ | Relacionado con `_referencia_menus.md`. |
| 21 | misas | 33 | 23 | 32 | ⭐ | API+pant+flujos revisados (ola 5). `operacion` corregida (7 `lista_data`, 12 `form_data`, 14 `mutacion`; desplegables y `ver_*_data` ya no mutación); Salida real payloads + doble `JSON.parse`; errores `_()` y linaje `apps/misas/*` (slices 3–10, dispatchers `update_encargos_*`/`cuadricula_update`); permisos `IdNomJefeResolver`/rol ctr/sacd/jefe. Ruta menú dre/ATENCIÓN SACD > Gestión de misas en 12 pant + flujos; fragmentos/modales/zonassacd «sin entrada». Regenerados openapi+ai+manual. |
| 22 | notas | 33 | 26 | 31 | ⭐ | API+pant+flujos revisados (ola 5). `operacion` corregida (~12 `lista_data`, ~14 `form_data`, 9 mutaciones, 1 `descarga`; builders con doble `JSON.parse`); errores `_()` reales; permisos `est`/frontend/`rstgr`; linaje `apps/notas/*`. Dominios: actas (CRUD/PDF/imprimir), notas persona (dossier 1011), tessera, asignaturas pendientes, informes STGR, comprobar datos. Ruta menú `_referencia_menus.md` en 10 pant + flujos principales (vest/ESTUDIOS); fragmentos/dossiers sin menú. Regenerados openapi+ai+manual. |
| 23 | pasarela | 21 | 12 | 14 | ⭐ | API+pant+flujos revisados. `operacion`: 4 `*_lista`→lista_data, 6 builders `*_data`→form_data (`exportar_actividades_data`→lista_data); mutaciones ok. Salida real payloads + doble `JSON.parse`; alias `valor`↔`activacion`/`nombre_actividad`; contribuciones validan 0–100; exportación acumula errores tarifa/activación. Permisos frontend+`$_SESSION['oPerm']` (exportar_que HTML: `des`/`vcsd`/`calendario`). Ruta menú `parametros_menu` y `exportar_que` (`_referencia_menus.md`); subpantallas/AJAX sin menú; `nombre_form.php` legacy sin callers (flujo vía `nombre_ajax`). Regenerados openapi+ai+manual. |
| 24 | personas | 9 | 6 | 7 | ⭐ | API+pant+flujos revisados. `operacion`: 5 builders `*_data`→lista_data/form_data (`personas_select_data`→lista_data; `home_persona_data`, `personas_editar_data`, `stgr_cambio_data`, `traslado_form_data`→form_data); 4 mutaciones ok. Salida real payloads + doble `JSON.parse`; avisos suaves `RegionStgrAviso` en select/home; mutaciones `data:"ok"`; errores `_()` reales; permisos `have_perm_oficina(sm|agd|sg|des|est|nax|dtor)` frontend+`permiso` en listado; eliminar solo `mi_delef`. Linaje `apps/personas/*`. Ruta menú `_referencia_menus.md` en `personas_que`/`personas_select` (variantes vsm/PERSONAS por `tabla`/`na`); resto sin menú directo. Regenerados openapi+ai+manual. |
| 25 | planning | 7 | 11 | 7 | ⬜ | |
| 26 | procesos | 23 | 12 | 17 | ⭐ | API+pant+flujos revisados (ola 6). `operacion`: 8 `form_data`, 6 `lista_data`, 9 mutaciones (antes 23 mutacion); Salida real payloads + doble `JSON.parse`; errores `_()` reales; permisos `have_perm_oficina`/`perm_jefe`/frontend+`$_SESSION['oPerm']`. Dominios: definición procesos (select/ver/update/clonar/regenerar), tipo activ-proceso, cambio fase masivo, dossier actividad_proceso, permisos fase (`usuario_perm_activ`). Ruta menú `_referencia_menus.md` en `procesos_select`, `tipo_activ_proceso`, `fases_activ_cambio` (+ variantes vest/vsm/dagd/vsg); fragmentos/dossiers sin menú. Regenerados openapi+ai+manual. |
| 27 | profesores | 6 | 6 | 6 | ⭐ | API+pant+flujos revisados (ola 6). `operacion`: 3 `lista_data` (`docencia`, `congresos`, `profesor_asignatura_ajax`); 3 `form_data` (`ficha_profesor_stgr`, `lista_por_departamentos`, `profesor_asignatura_que`); sin mutaciones. Salida real payloads + doble `JSON.parse`; `lista_por_departamentos` ramas `modo=filtro`/`lista`; ficha con `aPerm`+`go_cosas_link_specs`; errores `_()` reales. Permisos frontend `have_perm_oficina('est')` en ficha + `PermDossier`; resto `$_SESSION['oPerm']`. Linaje `apps/profesores/*`. Ruta menú vest/ESTUDIOS (`claustro`, `profesor para asignatura`); `docencia`/`congresos` solo legacy stgr2; ficha/ajax sin menú. Regenerados openapi+ai+manual. |
| 28 | shared | 6 | 2 | 6 | ⭐ | API+pant+flujos revisados (ola 6). Transversal `tablaDB`: `operacion` corregida (4 builders→lista_data/form_data; `tablaDB_update`→mutacion); Salida real payloads + doble `JSON.parse`; errores `DatosUpdateRepo`/`_()`; permisos frontend `permiso`+`$_SESSION['oPerm']`; linaje inline controllers+`Datos*Repo`. `locales_posibles`→form_data (`a_locales`). Ruta menú: `tablaDB_lista_ver` ~25 variantes `_referencia_menus.md` por `clase_info`; formulario/depende/locales «sin entrada». Regenerados openapi+ai+manual. |
| 29 | tablonanuncios | 1 | 1 | 1 | ✅ | API revisada (`anuncio_delete`): entrada `sel`/`uuid_item`, errores reales, permisos frontend `HashFront`+`$_SESSION['oPerm']`. Frontend en `public/portada.php`. |
| 30 | ubis | 40 | 35 | 36 | ⭐ | API+pant+flujos revisados (ola 6). `operacion` corregida (~14 `lista_data`, ~18 `form_data`, 12 mutaciones; builders `*_data` ya no mutación); Salida real payloads + doble `JSON.parse`; errores `_()` reales; permisos `UbiPermisos`/`have_perm_oficina(scdl|scl|vcsd|des|admin_sv)`; dominios: búsqueda/listado ubis, ficha edición (CentroDl/Ex, Casa), direcciones, teleco, centros masivo, calendario CDC, traslado DL. Ruta menú `_referencia_menus.md` en 6 pant principales (`ubis_buscar`, `list_ctr`, `centros_que`, `calendario_periodos`, `lista_ctrs`); fragmentos/dossiers/desplegables «sin entrada». Regenerados openapi+ai+manual. Script: `aplicar_revision_ubis.php`. |
| 31 | ubiscamas | 9 | 5 | 5 | ⭐ | API+pant+flujos revisados (ola 7). `operacion`: `actividad_habitaciones_lista`→lista_data; 2 `form_data`; 6 mutaciones (`update_cama_asistente`/`update_solo_vip` con `requiere_hashb:true`, JSON directo). Salida real payloads + doble `JSON.parse`; errores `_()`/`Exception` reales; permisos frontend+dossier actividad/CDC. Dominios: habitaciones/camas CDC (`habitacion_form`, dossier `select_habitaciones_cdc`), asignación camas actividad (`lista_habitaciones` vía `actividades.js` que=camas). Ruta menú: sin entrada en `_referencia_menus.md` (acceso dossier). Regenerados openapi+ai+manual. Script: `aplicar_revision_ubiscamas.php`. |
| 32 | usuarios | 44 | 24 | 33 | ⭐ | API+pant+flujos revisados (ola 7 FINAL). `operacion` corregida (~10 `lista_data`, ~12 `form_data`, 22 mutaciones; builders ya no mutación); Salida real payloads + doble `JSON.parse` (`usuario_form` JsonResponse); errores `_()` reales; permisos id_role≤3/HashB/frontend+`$_SESSION['oPerm']`; 7 casos `application/` + resto inline controllers. Dominios: login/sesión/2FA, preferencias, admin usuarios/roles/grupos, perm menú/actividad, recovery mail. Ruta menú ADMIN LOCAL > usuarios web en 3 pant principales; login/recovery/preferencias sin menú admin. Script: `aplicar_revision_usuarios.php`. Regenerados openapi+ai+manual. |
| 33 | zonassacd | 9 | 6 | 5 | ⭐ | API+pant+flujos revisados (ola 7 FINAL). `operacion`: 2 `form_data` (zona_sacd/ctr page), 2 `lista_data` (listas), 2 mutaciones (update), 2 rutas muertas (zona_*_ajax sin controller); Salida real payloads + doble `JSON.parse`; errores `_()` reales; permisos `perm_des` vía `have_perm_oficina(des|vcsd)` frontend+sesión; modal días vía `misas/zona_sacd_datos_*`; `zona_sacd_lista_ajax?que=get_lista_tot` consume lista_tot. Ruta menú ATENCIÓN SACD > Gestión de zonas (Zonas-sacd, Zonas-ctr, Lista sacd-zona); fragmentos AJAX «sin entrada». Linaje `apps/zonassacd/*`. Regenerados openapi+ai+manual. Script: `aplicar_revision_zonassacd.php`. |

**Lagunas de cobertura conocidas:**
- `devel_codegen` (existe en `frontend/` y `src/`) no tiene catálogo ni `docs/ai`. Herramienta
  interna de generación de código; documentar solo si se decide.
