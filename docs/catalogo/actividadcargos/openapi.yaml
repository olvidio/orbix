openapi: "3.1.0"
info:
  title: "Orbix API - actividadcargos"
  version: "0.1.0"
  description: |
    OpenAPI generado desde `docs/catalogo`. Revisar obligatoriedad de campos y ejemplos antes de publicarlo como contrato estable.
  license:
    name: "Proprietary"
    identifier: "LicenseRef-Proprietary"
servers:
  - url: "/"
security:
  - OrbixSession: []
paths:
  "/src/actividadcargos/cargo_editar":
    post:
      operationId: "post_actividadcargos_cargo_editar"
      summary: "Cargo Editar"
      description: |
        Edita un `ActividadCargo` existente.
        
        Fuente catalogo: `docs/catalogo/actividadcargos/api/cargo_editar.md`
      tags:
        - "actividadcargos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                asis:
                  type: "string"
                asis_presente:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/actividadcargos/cargo_eliminar":
    post:
      operationId: "post_actividadcargos_cargo_eliminar"
      summary: "Cargo Eliminar"
      description: |
        Elimina un `ActividadCargo` y, si procede, su `Asistente`.
        
        Fuente catalogo: `docs/catalogo/actividadcargos/api/cargo_eliminar.md`
      tags:
        - "actividadcargos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/actividadcargos/cargo_nuevo":
    post:
      operationId: "post_actividadcargos_cargo_nuevo"
      summary: "Cargo Nuevo"
      description: |
        Crea un `ActividadCargo`.
        
        Fuente catalogo: `docs/catalogo/actividadcargos/api/cargo_nuevo.md`
      tags:
        - "actividadcargos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/actividadcargos/form_cargos_de_actividad_data":
    post:
      operationId: "post_actividadcargos_form_cargos_de_actividad_data"
      summary: "Form Cargos De Actividad Data"
      description: |
        Descripcion funcional pendiente de revisar.
        
        Fuente catalogo: `docs/catalogo/actividadcargos/api/form_cargos_de_actividad_data.md`
      tags:
        - "actividadcargos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/actividadcargos/form_cargos_personas_en_actividad_data":
    post:
      operationId: "post_actividadcargos_form_cargos_personas_en_actividad_data"
      summary: "Form Cargos Personas En Actividad Data"
      description: |
        Descripcion funcional pendiente de revisar.
        
        Fuente catalogo: `docs/catalogo/actividadcargos/api/form_cargos_personas_en_actividad_data.md`
      tags:
        - "actividadcargos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
components:
  securitySchemes:
    OrbixSession:
      type: apiKey
      in: cookie
      name: PHPSESSID
  schemas:
    OrbixStandardResponseStringData:
      type: object
      required:
        - success
        - data
      properties:
        success:
          type: boolean
        mensaje:
          type: string
        data:
          oneOf:
            - type: string
            - type: object
            - type: array
              items: {}
          description: "En ContestarJson::enviar, los arrays/objetos suelen llegar codificados como string JSON por compatibilidad."
