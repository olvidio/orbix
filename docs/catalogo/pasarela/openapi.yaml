openapi: "3.1.0"
info:
  title: "Orbix API - pasarela"
  version: "0.1.0"
  description: |
    OpenAPI generado desde `docs/catalogo`. Revisar obligatoriedad de campos y ejemplos antes de publicarlo como contrato estable.
  license:
    name: "Proprietary"
    identifier: "LicenseRef-Proprietary"
servers:
  - url: "/"
security:
  - OrbixSession: []
paths:
  "/src/pasarela/activacion_default_data":
    post:
      operationId: "post_pasarela_activacion_default_data"
      summary: "Activacion Default Data"
      description: |
        Devuelve el valor por defecto del parámetro `fecha_activacion` para el formulario `form_default`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/activacion_default_data.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/activacion_default_guardar":
    post:
      operationId: "post_pasarela_activacion_default_guardar"
      summary: "Activacion Default Guardar"
      description: |
        Actualiza el valor por defecto del parámetro `fecha_activacion`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/activacion_default_guardar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/activacion_excepcion_eliminar":
    post:
      operationId: "post_pasarela_activacion_excepcion_eliminar"
      summary: "Activacion Excepcion Eliminar"
      description: |
        Elimina una excepción de `fecha_activacion` para un tipo de actividad.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/activacion_excepcion_eliminar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/activacion_excepcion_guardar":
    post:
      operationId: "post_pasarela_activacion_excepcion_guardar"
      summary: "Activacion Excepcion Guardar"
      description: |
        Alta o edición de una excepción de `fecha_activacion` para un `id_tipo_activ`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/activacion_excepcion_guardar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/activacion_lista":
    post:
      operationId: "post_pasarela_activacion_lista"
      summary: "Activacion Lista"
      description: |
        Devuelve el listado del parámetro `fecha_activacion`: valor por defecto y excepciones por tipo de actividad.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/activacion_lista.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_no_duerme_default_data":
    post:
      operationId: "post_pasarela_contribucion_no_duerme_default_data"
      summary: "Contribucion No Duerme Default Data"
      description: |
        Valor por defecto de `contribucion_no_duerme` para `form_default`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_no_duerme_default_data.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_no_duerme_default_guardar":
    post:
      operationId: "post_pasarela_contribucion_no_duerme_default_guardar"
      summary: "Contribucion No Duerme Default Guardar"
      description: |
        Actualiza el porcentaje por defecto de contribución «no duerme».
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_no_duerme_default_guardar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_no_duerme_excepcion_eliminar":
    post:
      operationId: "post_pasarela_contribucion_no_duerme_excepcion_eliminar"
      summary: "Contribucion No Duerme Excepcion Eliminar"
      description: |
        Elimina excepción de contribución no duerme.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_no_duerme_excepcion_eliminar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_no_duerme_excepcion_guardar":
    post:
      operationId: "post_pasarela_contribucion_no_duerme_excepcion_guardar"
      summary: "Contribucion No Duerme Excepcion Guardar"
      description: |
        Alta/edición de excepción de contribución no duerme por tipo.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_no_duerme_excepcion_guardar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_no_duerme_lista":
    post:
      operationId: "post_pasarela_contribucion_no_duerme_lista"
      summary: "Contribucion No Duerme Lista"
      description: |
        Listado del parámetro `contribucion_no_duerme` (porcentaje 0–100).
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_no_duerme_lista.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_reserva_default_data":
    post:
      operationId: "post_pasarela_contribucion_reserva_default_data"
      summary: "Contribucion Reserva Default Data"
      description: |
        Valor por defecto de `contribucion_reserva` para `form_default`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_reserva_default_data.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_reserva_default_guardar":
    post:
      operationId: "post_pasarela_contribucion_reserva_default_guardar"
      summary: "Contribucion Reserva Default Guardar"
      description: |
        Actualiza el porcentaje por defecto de contribución reserva.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_reserva_default_guardar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_reserva_excepcion_eliminar":
    post:
      operationId: "post_pasarela_contribucion_reserva_excepcion_eliminar"
      summary: "Contribucion Reserva Excepcion Eliminar"
      description: |
        Elimina excepción de contribución reserva.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_reserva_excepcion_eliminar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_reserva_excepcion_guardar":
    post:
      operationId: "post_pasarela_contribucion_reserva_excepcion_guardar"
      summary: "Contribucion Reserva Excepcion Guardar"
      description: |
        Alta/edición de excepción de contribución reserva por tipo.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_reserva_excepcion_guardar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/contribucion_reserva_lista":
    post:
      operationId: "post_pasarela_contribucion_reserva_lista"
      summary: "Contribucion Reserva Lista"
      description: |
        Listado del parámetro `contribucion_reserva` (porcentaje reserva).
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/contribucion_reserva_lista.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/exportar_actividades_data":
    post:
      operationId: "post_pasarela_exportar_actividades_data"
      summary: "Exportar Actividades Data"
      description: |
        Genera cabeceras y filas para exportar actividades al exterior según filtros.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/exportar_actividades_data.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/exportar_que_actividad_tipo_html":
    post:
      operationId: "post_pasarela_exportar_que_actividad_tipo_html"
      summary: "Exportar Que Actividad Tipo Html"
      description: |
        HTML del widget selector de tipo de actividad en «exportar actividades».
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/exportar_que_actividad_tipo_html.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/nombre_excepcion_eliminar":
    post:
      operationId: "post_pasarela_nombre_excepcion_eliminar"
      summary: "Nombre Excepcion Eliminar"
      description: |
        Elimina el nombre particular de un tipo de actividad.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/nombre_excepcion_eliminar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/nombre_excepcion_guardar":
    post:
      operationId: "post_pasarela_nombre_excepcion_guardar"
      summary: "Nombre Excepcion Guardar"
      description: |
        Alta/edición del nombre de actividad para un tipo concreto.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/nombre_excepcion_guardar.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/nombre_lista":
    post:
      operationId: "post_pasarela_nombre_lista"
      summary: "Nombre Lista"
      description: |
        Listado del parámetro `nombre` (nombres particulares por tipo de actividad).
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/nombre_lista.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/pasarela/tipo_activ_txt_data":
    post:
      operationId: "post_pasarela_tipo_activ_txt_data"
      summary: "Tipo Activ Txt Data"
      description: |
        Texto descriptivo del tipo de actividad (sf/sv + asistentes + actividad).
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/pasarela/api/tipo_activ_txt_data.md`
      tags:
        - "pasarela"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
components:
  securitySchemes:
    OrbixSession:
      type: apiKey
      in: cookie
      name: PHPSESSID
  schemas:
    OrbixStandardResponseStringData:
      type: object
      required:
        - success
        - data
      properties:
        success:
          type: boolean
        mensaje:
          type: string
        data:
          oneOf:
            - type: string
            - type: object
            - type: array
              items: {}
          description: "En ContestarJson::enviar, los arrays/objetos suelen llegar codificados como string JSON por compatibilidad."
