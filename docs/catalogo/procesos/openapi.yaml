openapi: "3.1.0"
info:
  title: "Orbix API - procesos"
  version: "0.1.0"
  description: |
    OpenAPI generado desde `docs/catalogo`. Revisar obligatoriedad de campos y ejemplos antes de publicarlo como contrato estable.
  license:
    name: "Proprietary"
    identifier: "LicenseRef-Proprietary"
servers:
  - url: "/"
security:
  - OrbixSession: []
paths:
  "/src/procesos/actividad_proceso_data":
    post:
      operationId: "post_procesos_actividad_proceso_data"
      summary: "Actividad Proceso Data"
      description: |
        Caso de uso: datos para la pantalla `actividad_proceso`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_data.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_activ:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ActividadProcesoDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ActividadProcesoDataDataResponse"
  "/src/procesos/actividad_proceso_generar":
    post:
      operationId: "post_procesos_actividad_proceso_generar"
      summary: "Actividad Proceso Generar"
      description: |
        Caso de uso: (re)genera las tareas del proceso asociado a un id_activ.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_generar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_activ:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/actividad_proceso_get":
    post:
      operationId: "post_procesos_actividad_proceso_get"
      summary: "Actividad Proceso Get"
      description: |
        Caso de uso: tareas del proceso para un id_activ (estructura + permiso edición).
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_get.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_activ:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ActividadProcesoGetDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ActividadProcesoGetDataResponse"
  "/src/procesos/actividad_proceso_update":
    post:
      operationId: "post_procesos_actividad_proceso_update"
      summary: "Actividad Proceso Update"
      description: |
        Caso de uso: guarda el estado (completado/observaciones) de una tarea del proceso.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_update.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                completado:
                  type: "string"
                id_item:
                  type: "integer"
                observ:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/actividad_que_fases_ajax":
    post:
      operationId: "post_procesos_actividad_que_fases_ajax"
      summary: "Actividad Que Fases Ajax"
      description: |
        Caso de uso: fases aplicables al tipo de actividad para checkboxes fases_on/fases_off.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_que_fases_ajax.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_propia:
                  type: "string"
                id_tipo_activ:
                  type: "string"
                selected:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ActividadQueFasesCuadroDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ActividadQueFasesCuadroDataResponse"
  "/src/procesos/fases_activ_cambio_get":
    post:
      operationId: "post_procesos_fases_activ_cambio_get"
      summary: "Fases Activ Cambio Get"
      description: |
        Caso de uso: fases posibles para id_tipo_activ y dl_propia (desplegable JSON).
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_get.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_propia:
                  type: "string"
                id_fase_sel:
                  type: "string"
                id_tipo_activ:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_FasesActivCambioGetDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_FasesActivCambioGetDataResponse"
  "/src/procesos/fases_activ_cambio_lista":
    post:
      operationId: "post_procesos_fases_activ_cambio_lista"
      summary: "Fases Activ Cambio Lista"
      description: |
        Caso de uso: datos estructurados para tabla de actividades candidatas a cambiar de fase.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_lista.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                accion:
                  type: "string"
                dl_propia:
                  type: "string"
                empiezamax:
                  type: "string"
                empiezamin:
                  type: "string"
                id_fase_nueva:
                  type: "string"
                id_tipo_activ:
                  type: "string"
                periodo:
                  type: "string"
                year:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/fases_activ_cambio_tipo_html":
    post:
      operationId: "post_procesos_fases_activ_cambio_tipo_html"
      summary: "Fases Activ Cambio Tipo Html"
      description: |
        Payload para fases_activ_cambio: HTML del selector tipo actividad.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_tipo_html.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_activ:
                  type: "string"
                sactividad:
                  type: "string"
                sactividad2:
                  type: "string"
                sasistentes:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_FasesActivCambioTipoActividadHtmlDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_FasesActivCambioTipoActividadHtmlDataDataResponse"
  "/src/procesos/fases_activ_cambio_update":
    post:
      operationId: "post_procesos_fases_activ_cambio_update"
      summary: "Fases Activ Cambio Update"
      description: |
        Caso de uso: aplica setCompletado a la fase nueva para actividades seleccionadas.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_update.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                accion:
                  type: "string"
                id_fase_nueva:
                  type: "string"
                sel:
                  type: "array"
                  items:
                    type: string
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_clonar":
    post:
      operationId: "post_procesos_procesos_clonar"
      summary: "Procesos Clonar"
      description: |
        Caso de uso: clona las tareas de un proceso de referencia al proceso indicado.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_clonar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_proceso:
                  type: "integer"
                id_tipo_proceso_ref:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_depende":
    post:
      operationId: "post_procesos_procesos_depende"
      summary: "Procesos Depende"
      description: |
        Caso de uso: opciones del desplegable de tareas dependientes de una fase.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_depende.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                acc:
                  type: "string"
                valor_depende:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosDependeDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosDependeDataResponse"
  "/src/procesos/procesos_eliminar":
    post:
      operationId: "post_procesos_procesos_eliminar"
      summary: "Procesos Eliminar"
      description: |
        Caso de uso: elimina una tarea_proceso por su id_item.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_eliminar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_item:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_get":
    post:
      operationId: "post_procesos_procesos_get"
      summary: "Procesos Get"
      description: |
        Caso de uso: estructura padres/hijos del árbol de fases del proceso.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_get.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_proceso:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosGetDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosGetDataResponse"
  "/src/procesos/procesos_get_listado":
    post:
      operationId: "post_procesos_procesos_get_listado"
      summary: "Procesos Get Listado"
      description: |
        Caso de uso: listado estructurado de fases/tareas del proceso.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_get_listado.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_proceso:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosGetListadoDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosGetListadoDataResponse"
  "/src/procesos/procesos_regenerar":
    post:
      operationId: "post_procesos_procesos_regenerar"
      summary: "Procesos Regenerar"
      description: |
        Caso de uso: regenera las tareas del proceso a partir de tareas_proceso.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_regenerar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_proceso:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_select_data":
    post:
      operationId: "post_procesos_procesos_select_data"
      summary: "Procesos Select Data"
      description: |
        Caso de uso: datos para la pantalla `procesos_select`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_select_data.md`
      tags:
        - "procesos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosSelectDataDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_ProcesosSelectDataDataResponse"
  "/src/procesos/procesos_update":
    post:
      operationId: "post_procesos_procesos_update"
      summary: "Procesos Update"
      description: |
        Caso de uso: guarda una tarea_proceso del proceso.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_update.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_fase:
                  type: "integer"
                id_fase_previa:
                  type: "array"
                  items:
                    type: string
                id_item:
                  type: "integer"
                id_of_responsable:
                  type: "integer"
                id_tarea:
                  type: "integer"
                id_tarea_previa:
                  type: "array"
                  items:
                    type: string
                id_tipo_proceso:
                  type: "integer"
                mensaje_requisito:
                  type: "array"
                  items:
                    type: string
                status:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_ver_data":
    post:
      operationId: "post_procesos_procesos_ver_data"
      summary: "Procesos Ver Data"
      description: |
        Caso de uso: datos para la pantalla `procesos_ver`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_ver_data.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_item:
                  type: "integer"
                mod:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/tipo_activ_proceso_asignar":
    post:
      operationId: "post_procesos_tipo_activ_proceso_asignar"
      summary: "Tipo Activ Proceso Asignar"
      description: |
        Caso de uso: asigna id_tipo_proceso al tipo de actividad (propio / no-propio).
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/tipo_activ_proceso_asignar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_activ:
                  type: "integer"
                id_tipo_proceso:
                  type: "integer"
                propio:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/tipo_activ_proceso_lista":
    post:
      operationId: "post_procesos_tipo_activ_proceso_lista"
      summary: "Tipo Activ Proceso Lista"
      description: |
        Caso de uso: listado de tipos de actividad con proceso propio / no-propio.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/tipo_activ_proceso_lista.md`
      tags:
        - "procesos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_TipoActivProcesoListaDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_TipoActivProcesoListaDataResponse"
  "/src/procesos/tipo_activ_proceso_lst_posibles":
    post:
      operationId: "post_procesos_tipo_activ_proceso_lst_posibles"
      summary: "Tipo Activ Proceso Lst Posibles"
      description: |
        Caso de uso: procesos posibles asignables a un id_tipo_activ.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/tipo_activ_proceso_lst_posibles.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_activ:
                  type: "integer"
                propio:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/usuario_perm_activ_ajax":
    post:
      operationId: "post_procesos_usuario_perm_activ_ajax"
      summary: "Usuario Perm Activ Ajax"
      description: |
        Caso de uso: opciones del desplegable fase_ref[] en usuario_perm_activ.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/usuario_perm_activ_ajax.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_propia:
                  type: "string"
                id_tipo_activ:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_UsuarioPermActivFasesDataResponse"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/procesos_UsuarioPermActivFasesDataResponse"
  "/src/procesos/usuario_perm_activ_data":
    post:
      operationId: "post_procesos_usuario_perm_activ_data"
      summary: "Usuario Perm Activ Data"
      description: |
        Caso de uso: datos para la pantalla usuario_perm_activ.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/usuario_perm_activ_data.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_propia:
                  type: "string"
                id_tipo_activ_txt:
                  type: "string"
                id_usuario:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
components:
  securitySchemes:
    OrbixSession:
      type: apiKey
      in: cookie
      name: PHPSESSID
  schemas:
    OrbixStandardResponseEnvelope:
      type: object
      required:
        - success
        - data
      properties:
        success:
          type: boolean
        mensaje:
          type: string
        data:
          description: "Ver schema concreto de cada operacion."
    procesos_ActividadProcesoDataData:
      type: object
      properties:
        id_activ:
          type: "string"
    procesos_ActividadProcesoDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_ActividadProcesoDataData"
    procesos_ActividadProcesoGetData:
      type: object
      properties:
        error:
          type: "string"
    procesos_ActividadProcesoGetDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_ActividadProcesoGetData"
    procesos_ActividadQueFasesCuadroData:
      type: object
      properties:
        a_fases:
          type: "string"
    procesos_ActividadQueFasesCuadroDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_ActividadQueFasesCuadroData"
    procesos_FasesActivCambioGetData:
      type: object
      properties:
        id:
          type: "string"
    procesos_FasesActivCambioGetDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_FasesActivCambioGetData"
    procesos_FasesActivCambioTipoActividadHtmlDataData:
      type: object
      properties:
        tipo_actividad_html:
          type: "string"
    procesos_FasesActivCambioTipoActividadHtmlDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_FasesActivCambioTipoActividadHtmlDataData"
    procesos_ProcesosDependeData:
      type: object
      properties:
        opciones:
          type: array
          items: {}
    procesos_ProcesosDependeDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_ProcesosDependeData"
    procesos_ProcesosGetData:
      type: object
      properties:
        aPadres:
          type: array
          items: {}
    procesos_ProcesosGetDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_ProcesosGetData"
    procesos_ProcesosGetListadoData:
      type: object
      properties:
        a_rows:
          type: "string"
    procesos_ProcesosGetListadoDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_ProcesosGetListadoData"
    procesos_ProcesosSelectDataData:
      type: object
      properties:
        a_tipos_proceso:
          type: array
          items: {}
    procesos_ProcesosSelectDataDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_ProcesosSelectDataData"
    procesos_TipoActivProcesoListaData:
      type: object
      properties:
        a_cabeceras:
          type: "string"
        a_tipos:
          type: "string"
    procesos_TipoActivProcesoListaDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_TipoActivProcesoListaData"
    procesos_UsuarioPermActivFasesData:
      type: object
      properties:
        opciones:
          type: array
          items: {}
    procesos_UsuarioPermActivFasesDataResponse:
      allOf:
        - $ref: "#/components/schemas/OrbixStandardResponseEnvelope"
        - type: object
          properties:
            data:
              oneOf:
                - type: string
                  description: "JSON codificado como string (compatibilidad legacy)."
                - $ref: "#/components/schemas/procesos_UsuarioPermActivFasesData"
    OrbixStandardResponseStringData:
      type: object
      required:
        - success
        - data
      properties:
        success:
          type: boolean
        mensaje:
          type: string
        data:
          oneOf:
            - type: string
            - type: object
            - type: array
              items: {}
          description: "En ContestarJson::enviar, los arrays/objetos suelen llegar codificados como string JSON por compatibilidad."
