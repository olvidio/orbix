openapi: "3.1.0"
info:
  title: "Orbix API - procesos"
  version: "0.1.0"
  description: |
    OpenAPI generado desde `docs/catalogo`. Revisar obligatoriedad de campos y ejemplos antes de publicarlo como contrato estable.
  license:
    name: "Proprietary"
    identifier: "LicenseRef-Proprietary"
servers:
  - url: "/"
security:
  - OrbixSession: []
paths:
  "/src/procesos/actividad_proceso_data":
    post:
      operationId: "post_procesos_actividad_proceso_data"
      summary: "Actividad Proceso Data"
      description: |
        Datos iniciales para la pantalla `actividad_proceso` (nombre de la actividad seleccionada).
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_data.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_activ"
              properties:
                id_activ:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/actividad_proceso_generar":
    post:
      operationId: "post_procesos_actividad_proceso_generar"
      summary: "Actividad Proceso Generar"
      description: |
        (Re)genera las tareas del proceso asociadas a una actividad.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_generar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_activ"
              properties:
                id_activ:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/actividad_proceso_get":
    post:
      operationId: "post_procesos_actividad_proceso_get"
      summary: "Actividad Proceso Get"
      description: |
        Listado de tareas del proceso de una actividad con permiso de edición por fila.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_get.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_activ"
              properties:
                id_activ:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/actividad_proceso_update":
    post:
      operationId: "post_procesos_actividad_proceso_update"
      summary: "Actividad Proceso Update"
      description: |
        Guarda el estado (completado/observaciones) de una tarea del proceso de actividad.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_proceso_update.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_item"
              properties:
                completado:
                  type: "string"
                id_item:
                  type: "integer"
                observ:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/actividad_que_fases_ajax":
    post:
      operationId: "post_procesos_actividad_que_fases_ajax"
      summary: "Actividad Que Fases Ajax"
      description: |
        Fases aplicables al tipo de actividad para los checkboxes `fases_on` / `fases_off`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/actividad_que_fases_ajax.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_activ"
              properties:
                dl_propia:
                  type: "string"
                id_tipo_activ:
                  type: "string"
                selected:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/fases_activ_cambio_get":
    post:
      operationId: "post_procesos_fases_activ_cambio_get"
      summary: "Fases Activ Cambio Get"
      description: |
        Desplegable JSON de fases posibles para cambio masivo de fase.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_get.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_activ"
              properties:
                dl_propia:
                  type: "string"
                id_fase_sel:
                  type: "string"
                id_tipo_activ:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/fases_activ_cambio_lista":
    post:
      operationId: "post_procesos_fases_activ_cambio_lista"
      summary: "Fases Activ Cambio Lista"
      description: |
        Datos estructurados para la tabla de actividades candidatas a cambiar de fase.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_lista.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_fase_nueva"
              properties:
                accion:
                  type: "string"
                dl_propia:
                  type: "string"
                empiezamax:
                  type: "string"
                empiezamin:
                  type: "string"
                id_fase_nueva:
                  type: "string"
                id_tipo_activ:
                  type: "string"
                periodo:
                  type: "string"
                year:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/fases_activ_cambio_tipo_html":
    post:
      operationId: "post_procesos_fases_activ_cambio_tipo_html"
      summary: "Fases Activ Cambio Tipo Html"
      description: |
        Fragmento HTML del selector de tipo de actividad en `fases_activ_cambio`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_tipo_html.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_tipo_activ:
                  type: "string"
                sactividad:
                  type: "string"
                sactividad2:
                  type: "string"
                sasistentes:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/fases_activ_cambio_update":
    post:
      operationId: "post_procesos_fases_activ_cambio_update"
      summary: "Fases Activ Cambio Update"
      description: |
        Aplica `setCompletado` a la fase nueva para las actividades seleccionadas.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/fases_activ_cambio_update.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_fase_nueva"
                - "sel"
              properties:
                accion:
                  type: "string"
                id_fase_nueva:
                  type: "string"
                sel:
                  type: "array"
                  items:
                    type: string
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_clonar":
    post:
      operationId: "post_procesos_procesos_clonar"
      summary: "Procesos Clonar"
      description: |
        Clona las tareas de un proceso de referencia al proceso indicado.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_clonar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_proceso"
                - "id_tipo_proceso_ref"
              properties:
                id_tipo_proceso:
                  type: "integer"
                id_tipo_proceso_ref:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_depende":
    post:
      operationId: "post_procesos_procesos_depende"
      summary: "Procesos Depende"
      description: |
        Opciones del desplegable de tareas dependientes de una fase en `procesos_ver`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_depende.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "acc"
              properties:
                acc:
                  type: "string"
                valor_depende:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_eliminar":
    post:
      operationId: "post_procesos_procesos_eliminar"
      summary: "Procesos Eliminar"
      description: |
        Elimina una `tarea_proceso` por su `id_item`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_eliminar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_item"
              properties:
                id_item:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_get":
    post:
      operationId: "post_procesos_procesos_get"
      summary: "Procesos Get"
      description: |
        Estructura padres/hijos del árbol de fases del proceso.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_get.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_proceso"
              properties:
                id_tipo_proceso:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_get_listado":
    post:
      operationId: "post_procesos_procesos_get_listado"
      summary: "Procesos Get Listado"
      description: |
        Listado estructurado de fases/tareas del proceso tipo.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_get_listado.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_proceso"
              properties:
                id_tipo_proceso:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_regenerar":
    post:
      operationId: "post_procesos_procesos_regenerar"
      summary: "Procesos Regenerar"
      description: |
        Regenera las instancias de tareas de actividad a partir de `tareas_proceso`.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_regenerar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_proceso"
              properties:
                id_tipo_proceso:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_select_data":
    post:
      operationId: "post_procesos_procesos_select_data"
      summary: "Procesos Select Data"
      description: |
        Datos iniciales para la pantalla `procesos_select`.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_select_data.md`
      tags:
        - "procesos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_update":
    post:
      operationId: "post_procesos_procesos_update"
      summary: "Procesos Update"
      description: |
        Guarda una `tarea_proceso` del proceso tipo.
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_update.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_item"
              properties:
                id_fase:
                  type: "integer"
                id_fase_previa:
                  type: "array"
                  items:
                    type: string
                id_item:
                  type: "integer"
                id_of_responsable:
                  type: "integer"
                id_tarea:
                  type: "integer"
                id_tarea_previa:
                  type: "array"
                  items:
                    type: string
                id_tipo_proceso:
                  type: "integer"
                mensaje_requisito:
                  type: "array"
                  items:
                    type: string
                status:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/procesos_ver_data":
    post:
      operationId: "post_procesos_procesos_ver_data"
      summary: "Procesos Ver Data"
      description: |
        Datos para el formulario de alta/edición de tarea de proceso (`procesos_ver`).
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/procesos_ver_data.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "mod"
              properties:
                id_item:
                  type: "integer"
                mod:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/tipo_activ_proceso_asignar":
    post:
      operationId: "post_procesos_tipo_activ_proceso_asignar"
      summary: "Tipo Activ Proceso Asignar"
      description: |
        Asigna un proceso tipo a un tipo de actividad (propio o no propio).
        
        Operacion: `mutacion`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/tipo_activ_proceso_asignar.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_activ"
              properties:
                id_tipo_activ:
                  type: "integer"
                id_tipo_proceso:
                  type: "integer"
                propio:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/tipo_activ_proceso_lista":
    post:
      operationId: "post_procesos_tipo_activ_proceso_lista"
      summary: "Tipo Activ Proceso Lista"
      description: |
        Listado de tipos de actividad con proceso propio y no propio asignados.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/tipo_activ_proceso_lista.md`
      tags:
        - "procesos"
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                _sin_parametros_detectados:
                  type: string
                  description: "No se han inferido campos desde el catalogo."
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/tipo_activ_proceso_lst_posibles":
    post:
      operationId: "post_procesos_tipo_activ_proceso_lst_posibles"
      summary: "Tipo Activ Proceso Lst Posibles"
      description: |
        Procesos posibles asignables a un tipo de actividad.
        
        Operacion: `lista_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/tipo_activ_proceso_lst_posibles.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_activ"
              properties:
                id_tipo_activ:
                  type: "integer"
                propio:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/usuario_perm_activ_ajax":
    post:
      operationId: "post_procesos_usuario_perm_activ_ajax"
      summary: "Usuario Perm Activ Ajax"
      description: |
        Opciones del desplegable `fase_ref[]` en permisos de usuario por tipo de actividad.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/usuario_perm_activ_ajax.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_tipo_activ"
              properties:
                dl_propia:
                  type: "string"
                id_tipo_activ:
                  type: "string"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
  "/src/procesos/usuario_perm_activ_data":
    post:
      operationId: "post_procesos_usuario_perm_activ_data"
      summary: "Usuario Perm Activ Data"
      description: |
        Datos para la pantalla de permisos de usuario por tipo de actividad.
        
        Operacion: `form_data`.
        
        Fuente catalogo: `docs/catalogo/procesos/api/usuario_perm_activ_data.md`
      tags:
        - "procesos"
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - "id_usuario"
              properties:
                dl_propia:
                  type: "string"
                id_tipo_activ_txt:
                  type: "string"
                id_usuario:
                  type: "integer"
      responses:
        "200":
          description: "Respuesta generada por Orbix."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
        "400":
          description: "Peticion no valida o error de validacion."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrbixStandardResponseStringData"
components:
  securitySchemes:
    OrbixSession:
      type: apiKey
      in: cookie
      name: PHPSESSID
  schemas:
    OrbixStandardResponseStringData:
      type: object
      required:
        - success
        - data
      properties:
        success:
          type: boolean
        mensaje:
          type: string
        data:
          oneOf:
            - type: string
            - type: object
            - type: array
              items: {}
          description: "En ContestarJson::enviar, los arrays/objetos suelen llegar codificados como string JSON por compatibilidad."
