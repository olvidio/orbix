Obsoleto: el flujo se dividió en [[devel/mapa_db_crear_esquema_que]] y [[devel/mapa_db_eliminar_esquema_que]].

`db_que.php` redirige a `db_crear_esquema_que.php` por compatibilidad con menús antiguos.