<table>
    <tr>
        <th>asignatura</th>
        <th>nb</th>
        <th>nc1</th>
        <th>nc2</th>
        <th>ntotal</th>
        <th>ab</th>
        <th>ac1</th>
        <th>ac2</th>
        <th>atotal</th>
    </tr>
    <?php
    foreach ($aPendientes as $Pendientes) {
        $asignatura = $Pendientes['def'];
        $nt = (int)($Pendientes['nb'] + $Pendientes['nc1'] + $Pendientes['nc2']);
        $at = (int)($Pendientes['ab'] + $Pendientes['ac1'] + $Pendientes['ac2']);
        ?>
        <tr>
            <td><?= htmlspecialchars(
                (string)($asignatura['nombre'] ?? '')
                    . ' ('
                    . (string)($asignatura['creditos'] ?? '')
                    . ' créditos, año '
                    . (string)($asignatura['year'] ?? '')
                    . ')',
                ENT_QUOTES,
                'UTF-8',
            ) ?></td>
            <td><?= (int)($Pendientes['nb'] ?? 0) ?></td>
            <td><?= (int)($Pendientes['nc1'] ?? 0) ?></td>
            <td><?= (int)($Pendientes['nc2'] ?? 0) ?></td>
            <td><?= $nt ?></td>
            <td><?= (int)($Pendientes['ab'] ?? 0) ?></td>
            <td><?= (int)($Pendientes['ac1'] ?? 0) ?></td>
            <td><?= (int)($Pendientes['ac2'] ?? 0) ?></td>
            <td><?= $at ?></td>
        </tr>
        <?php
    }
    ?>
</table>
