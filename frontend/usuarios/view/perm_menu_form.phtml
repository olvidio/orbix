<?php

use frontend\shared\config\AppUrlConfig;
use frontend\shared\permisos\MenuPermDlRadiosHtml;

?>
<script>
    fnjs_grabar = function (formulario) {
        var url = '<?= AppUrlConfig::srcBrowserUrl('/src/usuarios/perm_menu_guardar') ?>';
        let datos = $(formulario).serialize();
        let request = $.ajax({
            data: datos,
            url: url,
            method: 'POST',
            dataType: 'json'
        });
        request.done(function (json) {
            if (json.success !== true) {
                alert(<?= json_encode(_("respuesta")) ?> + ': ' + json.mensaje);
            } else {
                <?= $oPosicion->jsNavAtras(1); ?>
            }
        });
    }
</script>
<?= $oPosicion->mostrarNavAtras(1); ?>
<h1><?= sprintf(_("Añadir nuevo permiso a %s"), $nombre) ?></h1>
<form id=pem_usuario name=perm_usuario action="" method="post">
    <?= $oHash->getCamposHtml(); ?>
    <br>
    <table>
        <tr>
            <td class=etiqueta><?= ucfirst(_("oficina o grupo")); ?>:</td>
            <td colspan=5>
                <?= MenuPermDlRadiosHtml::cuadrosRadio('menu_perm', $menu_perm, $menu_perm_dl_map) ?>
            </td>
        </tr>
    </table>
    <br>
    <input type=button onclick="fnjs_grabar(this.form);" value=<?= _("guardar") ?>>
</form>
