<div id="div_select_zona">
    <form id="frm_zona_ctr" name="frm_zona_ctr" action="" method="post">
        {{ oHash.getCamposHtml|raw }}
        <table>
            <thead>
            <tr>
                <th class=titulo_inv colspan=4>{{ "Plan de Misas de un sacd"|trans|capitalize }}:&nbsp;&nbsp;&nbsp;
                    {{ oDesplSacd.desplegable|raw }}
                </th>
            </tr>
            <tr>
            {{ oFormP.getHtml()|raw }}
            </tr>
            </thead>
        </table>
    </form>
</div>

<br>
<div id="div_cuadricula">
</div>

<script>
    fnjs_ver_plan_sacd = function () {
        let id_sacd = $('#id_sacd').val();
        if (id_sacd) {
            let url = '{{ url_buscar_plan_sacd }}';
    	    var periodo=$('#periodo').val();
    	    var empiezamax=$('#empiezamax').val();
    	    var empiezamin=$('#empiezamin').val();

            let parametros = 'id_sacd=' + id_sacd + '&periodo=' + periodo + '&empiezamin=' + empiezamin + '&empiezamax=' + empiezamax + '{{ h_plan_sacd|raw }}';

            $.ajax({
                url: url,
                type: 'post',
                data: parametros,
                dataType: 'html'
            })
                .done(function (rta_txt) {
                    $('#div_cuadricula').html(rta_txt);
                });
        }
    }
</script>
