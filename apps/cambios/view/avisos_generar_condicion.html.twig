<ul>
    <li class="comentario">
	{{ "Para los avisos por mail, debe existir una linea en el crontab del servidor externo 
	(conexión a internet)."|trans|raw }}
	{{ "Es un cron distinto para cada dl (sv y sf)."|trans|raw }}
	</li>
	<li class="comentario">
	{{ "Si no está instalado el módulo de procesos, las fases de los avisos son los estados de la actividad."|trans|raw }}
	</li>
	<li class="comentario">
	{{ "Si el cambio es en una actividad de otra dl, la tabla de avisos no se genera automáticamente 
	(si se hace para la dl que ha hecho el cambio) Hay que hacerlo manual, o por cron, 
	o esperar a que se genere por algún cambio de la propia dl."|trans|raw }}
	</li>
	<li class="comentario">
	{{ "El sistema no es perfecto. Si hay dudas, hay que mirar la web para ver como está.
	Por ejemplo (para cambios en otras dl) puede ser que apunte cambios que se han hecho en una fase en la que no se debe avisar,
	 y al hacer otro cambio en una fase que si debe avisar, puede anotar el cambio que se había reallizado con anterioridad..."|trans|raw }}
	</li>
</ul>
<div id="condiciones" class="no_print">
	<form name="frm_cond" id="frm_cond" action="apps/cambios/controller/avisos_generar.php" method="post">
	{{ oHashCond.getCamposHtml()|raw }}
	{{ "usuario"|trans|raw }}:
	{{ oDesplUsuarios.desplegable()|raw }}
	{{  "tipo de aviso"|trans|raw }}:
	{{ oDesplTiposAviso.desplegable()|raw }}
	<input type="button" onclick="fnjs_enviar_formulario('#frm_cond','#exportar')" value="{{ "buscar"|trans|raw }}">
	</form>
</div>
<br><br>